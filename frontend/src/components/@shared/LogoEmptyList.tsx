import styled from '@emotion/styled';
import LogoEmptyList from '../../assets/images/Empty_List_Logo.svg';

const IconEmptyList = () => {
  return <Img src={LogoEmptyList} />;
};

const Img = styled.img`
  width: 70px;
`;
export default IconEmptyList;
